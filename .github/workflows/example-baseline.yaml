
name: Baseline example.com

on:
  workflow_dispatch:

jobs:
  zap_scan:
    runs-on: ubuntu-latest
    name: Baseline example.com
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-baseline@master
        with:
          target: 'https://www.example.com/'
          cmd_options: '-d'
          docker_name: 'psiinon/zap2docker-aftest'
