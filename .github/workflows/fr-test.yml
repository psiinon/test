name: ZAP vs Firing Range TEST

on: 
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
    - name: Test
      run: |
        docker run -v $(pwd):/zap/wrk/:rw -t owasp/zap2docker-live pwd > test.out
        cat test.out
